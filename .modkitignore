# Stays in the repo, out of the distributed package: the suite requires
# engine modules, which reads as a private require against the shipped
# archive (CONTRIBUTING-mods.md "What the PR must contain", 2).
tests/running_shoes_test.lua
